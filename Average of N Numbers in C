#include <stdio.h>
int main() {
    int n; float sum = 0, num;
    printf("How many numbers? ");
    scanf("%d", &n);
    for(int i=0; i<n; i++) {
        printf("Enter number %d: ", i+1);
        scanf("%f", &num);
        sum += num;
    }
    printf("Average = %.2f", sum/n);
    return 0;
}
