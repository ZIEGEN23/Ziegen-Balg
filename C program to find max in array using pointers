#include <stdio.h>
int main() {
    int n, max; scanf("%d", &n);
    int a[n], *p = a;
    for(int i=0; i<n; i++) scanf("%d", p+i);
    max = *p;
    for(int i=1; i<n; i++) if(*(p+i) > max) max = *(p+i);
    printf("Max: %d", max);
    return 0;
}
