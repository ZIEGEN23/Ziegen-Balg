#include <stdio.h>
int main() {
    int n = 29, i, flag = 0;
    for(i=2; i<=n/2; ++i) {
        if(n%i==0) { flag=1; break; }
    }
    (flag==0) ? printf("Prime") : printf("Not Prime");
    return 0;
}
