#include <stdio.h>
int main() {
    char s[100]; int v=0, c=0;
    scanf("%s", s);
    for(int i=0; s[i]; i++) {
        if(s[i]=='a'||s[i]=='e'||s[i]=='i'||s[i]=='o'||s[i]=='u') v++;
        else c++;
    }
    printf("Vowels: %d, Consonants: %d", v, c);
    return 0;
}
